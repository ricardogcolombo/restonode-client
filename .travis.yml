language: node_js
install:
  - npm install -g yarn
  - yarn install
node_js:
  -"node"
  -"8"
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
cache:
  yarn: true
  directories:
    - node_modules
